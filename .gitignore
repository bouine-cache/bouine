# Go build artifacts
/bin/
/dist/
*.exe
*.test
*.out
*.prof
*.pprof
*.cover
coverage.*
cover.html

# Go workspace local sums
go.work.sum

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# bouine local state
/var/
/tmp/
/.crush/
HANDOFF.md
test/integration/.tls/

# Benchmark outputs (current.txt is regenerated; baseline.txt is tracked)
/bench/results/current.txt

# Conformance harness (cloned at runtime, results regenerated)
/test/cachetests/.cache-tests/
/test/cachetests/results/

# Generated artifacts
/docs/_site/
/web/dashboard/node_modules/
/web/dashboard/dist/
/package-lock.json
/package.json

# Test data fuzz corpora — keep only the seed corpus, not the cached one
testdata/fuzz/*/

# Local environment overrides
*.local.yaml
*.local.yml
.env
.env.local

# Local-only plan — followed but never committed or pushed
/docs/plans/open-sourcing.md

/origin

# Local perf/soak harness outputs and compiled binaries — not tracked
/test/perf/results/
/test/soak/results/
/test/perf/mock-origin
/test/soak/mock-origin
