apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bouine.fullname" . }}-config
  labels:
    {{- include "bouine.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
